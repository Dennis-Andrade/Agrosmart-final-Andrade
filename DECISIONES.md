# 🧭 DECISIONES.md — Bitácora de diseño

> **Instrucciones.** Completa **una entrada por fase**, en **primera persona** y
> **refiriéndote a tu propio código**: nombres reales de tus clases, tu tabla, tus
> líneas, tu salida real de terminal.
>
> ❌ **No puntúa** una justificación genérica que podría pegarse en cualquier proyecto
> (ej.: *"usé boundedElastic porque es una buena práctica para operaciones bloqueantes"*).
> ✅ **Sí puntúa** una justificación anclada a tu código (ej.: *"en `ProductoService`
> línea 34 envolví `productoRepository.findAll()` porque Hibernate abre la conexión
> JDBC en el hilo llamante; al probarlo sin `subscribeOn` vi en el log el hilo
> `reactor-http-nio-2`, que es el event loop de Netty"*).
>
> Estas mismas preguntas se te harán en la **defensa oral**.

---

## Datos

- **Nombre: Dennis Alexander Andrade Guaman**
- **Cédula:1751025238**
- **NN (dos últimos dígitos):89**
- **Categoría asignada (según el último dígito):Quinua**

---

## Fase 1 — Configuración y perfiles

**1.1** ¿Qué archivo activa el perfil `prod` y qué línea exacta lo hace?

>El perfil prod se activa en application.propierties en la linea 2 para ser especifico spring.profiles.active=prod ya que esa linea ayuda al proyecto a saber donde es que están las configuraciones especificas de producción como lo es el puerto o la ia LangChain4j.

**1.2** Pega la línea del log de arranque donde se ve tu puerto y el perfil activo.

```
2026-07-31T17:13:57.168-05:00  INFO 5120 --- [agrosmart] [           main]
e.e.espe.agrosmart.AgroSmartApplication  : The following 1 profile is
active: "prod"

2026-07-31T17:14:01.259-05:00  INFO 5120 --- [agrosmart] [           main]
o.s.b.web.embedded.netty.NettyWebServer  : Netty started on port 8138
(http)
```

**1.3** ¿Qué habría pasado si dejabas `ddl-auto=create-drop` en lugar de `update`?
Responde pensando en tus datos sembrados.

>Practicamente Hibernate crearia la tabla cuando arranque y la eliminaria al apagar la aplicacion y claro que esto seria perjudicial para los datos sembrados ya que los 5 productos no quedarian persistidos entre ejecucion y ejecucion dando siempre un productoRepository.count() en 0 resultando en que la base simplemente no conserva nada despues de apagar el programa

**1.4** ¿Levantaste PostgreSQL con `compose.yaml` (Opción A) o con una instalación local
(Opción B)? ¿Qué ventaja tiene la que elegiste?

>Lo levante con compose.yaml ya que en la linea 2 se pone lo de postgres: y su imagen en la linea de abajo como image: postgres:16-alpine y lo hago de esta forma para que el mismo archivo defina todo osea la base de agrosmart_db (linea 6), el usuario agrosmart (linea 7), su contraseña (linea 8) y claro su puerto 5432 (linea 10) y me evito depender de una instalación manual de postgresql configurar y tal a cada rato.

---

## Fase 2 — Persistencia con JPA/Hibernate

**2.1** ¿Cuál es el nombre exacto de tu tabla y de dónde salió ese nombre?

>La tabla se llama tbl_productos_base_38 y salio de los requerimientos del examen junto claro con los dos ultimos digitos de mi cedula osea 17510252**38**

**2.2** Pega la salida de `psql -d agrosmart_db -c "\d tbl_productos_base_NN"` y
señala dónde se ve la restricción `unique` y el `length` de 120.

denis@Dennis:~/pa/Agrosmart-final-Andrade$ docker exec -i agrosmart-postgres psql -U agrosmart -d agrosmart_db -c "\d
  tbl_productos_base_38"
                                  Table "public.tbl_productos_base_38"
        Column        |          Type          | Collation | Nullable |             Default
----------------------+------------------------+-----------+----------+----------------------------------
 id_producto          | bigint                 |           | not null | generated by default as identity
 categoria            | character varying(40)  |           |          |
 correos_notificacion | character varying(500) |           |          |
 nombre_producto      | character varying(120) |           | not null |
 precio_usd           | numeric(10,2)          |           |          |
 stock_kg             | integer                |           | not null |
Indexes:
    "tbl_productos_base_38_pkey" PRIMARY KEY, btree (id_producto)
    **"uk5ay4rjiqgab68jqcjy82uktu9" UNIQUE CONSTRAINT, btree (nombre_producto)**

**2.3** ¿Por qué usaste `BigDecimal` y no `double` para `precio_usd`? Relaciónalo con el
tipo que generó Hibernate en PostgreSQL.

>Use el BigDecimal porque estoy manejando precios en dinero osea el precio_usd y ahi aplicarle un double es imposible porque usar flotantes binarios es completamente impresisos cuando se habla de registrar dinero.
>Por eso se tiene en ProductoEntity.java en su linea 24 el private BigDecimal precioUsd; y termino de configurar su precision en ProductoEntity.java para la linea 23 con  @Column(name = "precio_usd", precision = 10, scale = 2) que me asegura al 100% que todo se esta guardando con numeros decimales exactos y sus 2 cifras decimales que con double no lo puedo hacer correctamente.

**2.4** ¿Cómo hiciste idempotente tu siembra y qué pasaría en el segundo arranque si no
lo fuera? (piensa en la restricción `unique` de `nombre_producto`)

>La hice indempotente en el archivo ProductoSeeder.java mas especifico en la linea 18 con el codigo if (productoRepository.count() > 0) {.
>Y si no fuera indempotente el segundo arranque volveria a ingresar el mismo producto y saltaria de inmediato un error con postgresql ya que el archivo ProductoEntity.java en su linea 21 deja muy en claro que @Column(name = "nombre_producto", length = 120, nullable = false, unique =true) osea simplemente postgresql rechazaria la insercion duplicada por la restriccion del UNIQUE CONSTRAINT que esta en el nombre_producto y simplemente fallaria durante el arranque.

---

## Fase 3 — Modelo inmutable y lógica funcional

**3.1** ¿Por qué tienes **dos** clases (`ProductoEntity` y `Producto`) en lugar de una?
¿Qué te impide hacer inmutable directamente la entidad de Hibernate?

>Dado que public class ProductoEntity { (linea 14) del archivo ProductoEntity.java es para JPA/Hibernate por que tiene el constructor vacio protegido y setters para que Hibernate pueda construir cuando lea o persista algun dato.
>En cambio public final class Producto { (linea 9) del archivo Producto.java se encarga del dominio osea es final ya que sus campos son private final y no tiene ningun setters haciendola inmutable.
>Respondiendo a la pregunta crear una sola clase resultaria en una de dos cosas o sacrificaba la inmutabilidad en beneficio de la Hibernate o complicaba brutalmente la entidad JPA resultando en la separacion de las dos entidades por el bien de todos.
>Me lo impide por completo el propio Hibernate con sus entidades JPA ya que tiene protected ProductoEntity() { (linea 30) del archivo ProductoEntity.java haciendo necesario el crear instancias de la entidad cuando se lea postgesql y ya ni tengo en cuenta el caos que seria con los setters que se usan para asignar valores y gestinar cambios resultando aun mas catastrofico para el proyecto.

**3.2** Escribe el código exacto de **tus dos** copias defensivas e indica en qué línea
está cada una.

>Primera copia archivo Producto.java linea 24:
this.correosNotificacion = new ArrayList<>(
          Objects.requireNonNull(correosNotificacion, "correosNotificacion
          no puede ser null")
);

>Me ayuda a evitar que una lista externa modifique el estado interno de un producto despues de construirlo.
>Segunda copia en el mismo archivo linea 48:
return Collections.unmodifiableList(new ArrayList<>(correosNotificacion));

>Me ayuda a evitar que quien recibe la lista no la pueda modificar directamente esa lista interna.

**3.3** ¿Por qué la copia defensiva **solo en el getter** no sería suficiente? Describe
el ataque concreto que quedaría abierto sobre **tu** clase.

>Porque el la lista original que entra por el constructor seria facilmente atacable osea en el archivo Producto.java en la linea 17 tengo el List<String> correosNotificacion que al no tener su copia defensiva el objeto producto guardaria exactamente la misma referencia del codigo externo haciendo que por mas que se devuelva una copia que no se pueda modificar ya esta hecho el daño sea vaciando la lista del producto o modificandola por que tendrian la misma referencia.
>A y tambien romperia la regla de && !producto.getCorreosNotificacion().isEmpty(); (linea 12) del archivo ProductoFilters.java donde solo es valido si tiene correo. 

**3.4** ¿Cómo implementaste `A_MAYUSCULAS` para no mutar el `Producto` recibido?

De una forma bastante sencilla ya que solo cree uno nuevo mas no modifique el que recibi osea use public static final Function<Producto, Producto> A_MAYUSCULAS = producto-> new Producto( (linea 18) del archivo ProductoFilters.java y lo trasformo mas abajito en la linea 20 con producto.getNombre().toUpperCase(Locale.ROOT), de esta forma no queda ningun setNombre suelto ni mutacion sobre el objeto recibido por ello la forma que implemente fue construir una nueva instancia.

## Fase 4 — Servicio reactivo y aislamiento del bloqueo

**4.1** Pega tu método `obtenerProductosComercializables()` completo.

Esta en el archivo ProductoService.java desde la linea 31: 
  public Flux<Producto> obtenerProductosComercializables() {
      // fromCallable retrasa la consulta JPA hasta que exista una
      suscripcion real.
      return Mono.fromCallable(productoRepository::findAll)
              // boundedElastic mueve Hibernate fuera del event loop de
              Netty.
              .subscribeOn(Schedulers.boundedElastic())
              // flatMapMany convierte la lista bloqueante ya obtenida en
              flujo reactivo.
              .flatMapMany(Flux::fromIterable)
              // map separa la entidad mutable de Hibernate del modelo
              inmutable de dominio.
              .map(ProductoMapper::toDominio)
              // map aplica la transformacion funcional sin mutar el
              producto recibido.
              .map(ProductoFilters.A_MAYUSCULAS)
              // filter deja pasar solo productos comercializables segun la
              regla central.
              .filter(ProductoFilters.IS_VALID)
              // doOnNext registra trazabilidad sin cambiar el elemento del
              flujo.
              .doOnNext(ProductoFilters.LOG_PRODUCTO)
              // defaultIfEmpty evita una respuesta vacia si todos los
              productos son invalidos.
              .defaultIfEmpty(PRODUCTO_GENERICO);
  }

**4.2** ¿Qué pasa **exactamente** si eliminas
`.subscribeOn(Schedulers.boundedElastic())` de ese método? Si lo probaste, indica qué
hilo aparecía en el log antes y después.

>Si en mi archivo ProductoService.java elimino la linea 34 de .subscribeOn(Schedulers.boundedElastic()) entonces en la linea 32 el productoRepository.findAll() ejecutaria el hilo HTTP de Netty sale TEMP findAll thread=reactor-http-epoll-2 bloqueando el event loop de webFlux.

**4.3** ¿Por qué `Mono.fromCallable(...)` y no `Mono.just(repository.findAll())`?
(pista: cuándo se ejecuta cada uno)

>Bueno la diferencia mas clara es que Mono.fromCallable(...) retrasa cualquier ejecucion hasta que alguien se suscriba a su flujo mientras que Mono.just(repository.findAll()) ejecuta su repository.findAll() de forma inmediata al construir su Mono.
>Osea cuando lo ejecuto yo osea mi archivo ProductoService.java en la linea 34 return Mono.fromCallable(productoRepository::findAll) me interesa que retrase cualquier ejecucion para que luego si pueda moverla a boundedElastic: algo que ocn el Mono.just(repository.findAll()) no se podria ya que tiene el repositorio activo.

**4.4** En **tu** código, ¿dónde usaste `defaultIfEmpty` y dónde `switchIfEmpty`, y por
qué no son intercambiables en esos dos lugares?

>Use defaultIfEmpty en el archivo ProductoService.java en la linea 48 asi: .defaultIfEmpty(PRODUCTO_GENERICO); y el switchIfEmpty esta en la linea 58 asi: .switchIfEmpty(Mono.error(new ProductoNoEncontradoException(id))); pero simplemente es imposible de intercambiar porque sus decisiones de negocio son distintas osea si esta vacio uno lanza que no hay productos comercializables y devuelve su respaldo pero en el otro significa que ese id en especifico no existe y lanza un error.

**4.5** ¿Por qué `doOnNext` no sirve para transformar el elemento, si aparentemente
"recibe" el producto?

> Porque como lo muestro en el archivo ProductoService.java linea 46: .doOnNext(ProductoFilters.LOG_PRODUCTO) doOnNext solo recibe el LOG_PRODUCTO que simplemente es un Consumer<Producto> no un Function<Producto tal y como lo defino en el archivo ProductoFilters.java en la linea 15 public static final Consumer<Producto> LOG_PRODUCTO = producto -> .Haciendole solo aceptar el producto pero no devuelve otro producto.

---

## Fase 5 — Módulo de IA con LangChain4j

**5.1** Pega tu interfaz `AgroSmartAIService` completa.

Mi AgroSmartAIService esta en el archivo AgroSmartAIService.java desde la primera linea: 
  package ec.edu.espe.agrosmart.service;

  import dev.langchain4j.service.UserMessage;
  import dev.langchain4j.service.V;
  import dev.langchain4j.service.spring.AiService;

  @AiService
  public interface AgroSmartAIService {

      @UserMessage("""
              Redacta una frase publicitaria de máximo 100 caracteres para
              vender \
              {{producto}} dirigido a {{audiencia}}.""")
      String generarPublicidad(@V("producto") String producto,
                               @V("audiencia") String audiencia);
  }

**5.2** ¿Qué hace `@V("producto")` y qué pasaría si lo quitaras dejando solo el
parámetro?

>En mi proyecto el @V("producto") ayuda al archivo AgroSmartAIService.java en la linea 13 a hacer String generarPublicidad(@V("producto") String producto, osea es una variable que se usa en el prompt de AgroSmartAIService.java en la linea 11 {{producto}} dirigido a {{audiencia}}.""") osea que si solo dejo el parametro String generarPublicidad(String producto, la IA LangChain4j no tiene como tal su asociacion explicita con {{producto}} y el promt fallaria al construir la llamada al servicio de IA.

**5.3** ¿En qué archivo y con qué líneas configuraste el modelo? ¿Por qué **no** hizo
falta declarar un `@Bean`?

>Mi modelo se encuentra en el archivo application-prod.properties en la linea 9 langchain4j.open-ai.chat-model.api-key=${OPENAI_API_KEY:demo} tambien en la linea 10   langchain4j.open-ai.chat-model.base-url=${OPENAI_BASE_URL:http://langchain4j.dev/demo/openai/v1} linea 11 angchain4j.open-ai.chat-model.model-name=${OPENAI_MODEL:gpt-4o-mini} y linea 12 langchain4j.open-ai.chat-model.timeout=30s.
>No hizo falta el @Bean por que mi proyecto ya trae su propio starter de la IA LangChain4j en el archivo pom.xml en su linea 45 <artifactId>langchain4j-open-ai-spring-boot-starter</artifactId> que simplemente lleyendo las properties lo configura solo.

**5.4** ¿Por qué la llamada a la IA también necesita `boundedElastic`, si no es una
consulta a base de datos?

>Osea en mi archivo PublicidadService.java en la linea 18 da return Mono.fromCallable(() -> agroSmartAIService.generarPublicidad(producto, audiencia)) que si no es consulta a base de datos pero sigue siendo una llamada potenciamente bloqueante osea que el hilo queda esperando que la IA por eso se mueve a boundedElastic de la linea 19 del mismo archivo .subscribeOn(Schedulers.boundedElastic()).

**5.5** Si tu proveedor devolvió un error durante el examen, pega el mensaje real y la
respuesta que produjo tu `onErrorResume`.

No recibi ningun error de mi proveedor

## Fase 6 — API reactiva con WebFlux

**6.1** Pega la salida real de tus cuatro `curl`.

denis@Dennis:~$  curl -s http://localhost:8138/api/productos/1
{"id":1,"nombre":"Quinua organica de altura","categoria":"Quinua","precioUsd":4.80,"correosNotificacion":["ventas@agrosmart.ec","exportaciones@agrosmart.ec"]}denis@Dennis:~curl -s http://localhost:8138/api/productos/1/1
{"id":1,"nombre":"Quinua organica de altura","categoria":"Quinua","precioUsd":4.80,"correosNotificacion":["ventas@agrosmart.ec","exportaciones@agrosmart.ec"]}denis@Dennis:~curl -i -s http://localhost:8138/api/productos/999999
HTTP/1.1 404 Not Found
Content-Type: application/json
Content-Length: 82

{"codigo":"PRODUCTO_NO_ENCONTRADO","mensaje":"No existe un producto con id: 9999"}denis@Dennis:~$
denis@Dennis:~$ curl -s "http://localhost:8138/api/agrosmart/publicidad?producto=Quinua%20organica&audiencia=tiendas%20de%20alimentacion%20saludable"
"Descubre el poder de la quinua orgánica: salud y sabor en cada bocado. ¡Impulsa tu tienda hoy!"

**6.2** ¿Cómo lograste que el id inexistente responda **404** y no 500?

>Lo logre solo modificando el archivo ProductoService.java en la linea 58 .switchIfEmpty(Mono.error(new ProductoNoEncontradoException(id))); modificando el no encontrado en una excepcion propia osea si findById(id) regresa vacio le lanzo de una la ProductoNoEncontradoException y se captura en el archivo ApiExceptionHandler.java en la linea 11 aqui @ExceptionHandler(ProductoNoEncontradoException.class dejandola como HTTP 404 una linea mas abajo en @ResponseStatus(HttpStatus.NOT_FOUND)

**6.3** ¿Qué pasaría si tu controlador devolviera `List<Producto>` en lugar de
`Flux<Producto>`? ¿Seguiría compilando? ¿Seguiría siendo no bloqueante?

>En mi proyecto cambiar en el archivo AgroSmartController.java en la linea 26 el public Flux<Producto> obtenerProductos() { por public List<Producto> obtenerProductos() { simplemente se romperia por que mi metodo seguira retornando a la linea 27 return productoService.obtenerProductosComercializables(); del archivo AgroSmartController.java y tampoco llegaria ni a considerarse endpoint no bloqueante

## Fase 7 — Pruebas unitarias

**7.1** Pega la salida real de tus pruebas (`./mvnw test` o `./gradlew test`).
MVNW TEST
[INFO] Scanning for projects...
[INFO]
[INFO] ----------------< ec.edu.espe:agrosmart-final-andrade >-----------------
[INFO] Building AgroSmart Final Andrade 0.0.1-SNAPSHOT
[INFO] --------------------------------[ jar ]---------------------------------
[INFO]
[INFO] --- maven-resources-plugin:3.3.1:resources (default-resources) @ agrosmart-final-andrade ---
[INFO] Copying 2 resources from src/main/resources to target/classes
[INFO] Copying 0 resource from src/main/resources to target/classes
[INFO]
[INFO] --- maven-compiler-plugin:3.14.1:compile (default-compile) @ agrosmart-final-andrade ---
[INFO] Nothing to compile - all classes are up to date.
[INFO]
[INFO] --- maven-resources-plugin:3.3.1:testResources (default-testResources) @ agrosmart-final-andrade ---
[INFO] skip non existing resourceDirectory /home/denis/pa/Agrosmart-final-Andrade/src/test/resources
[INFO]
[INFO] --- maven-compiler-plugin:3.14.1:testCompile (default-testCompile) @ agrosmart-final-andrade ---
[INFO] Nothing to compile - all classes are up to date.
[INFO]
[INFO] --- maven-surefire-plugin:3.5.4:test (default-test) @ agrosmart-final-andrade ---
[INFO] Using auto detected provider org.apache.maven.surefire.junitplatform.JUnitPlatformProvider
[INFO]
[INFO] -------------------------------------------------------
[INFO]  T E S T S
[INFO] -------------------------------------------------------
[INFO] Running ec.edu.espe.agrosmart.domain.ProductoFiltersTest
[INFO] Tests run: 3, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 0.036 s -- in ec.edu.espe.agrosmart.domain.ProductoFiltersTest
[INFO] Running ec.edu.espe.agrosmart.domain.ProductoTest
[INFO] Tests run: 3, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 0.007 s -- in ec.edu.espe.agrosmart.domain.ProductoTest
[INFO] Running ec.edu.espe.agrosmart.service.ProductoServiceTest
Mockito is currently self-attaching to enable the inline-mock-maker. This will no longer work in future releases of the JDK. Please add Mockito as an agent to your build as described in Mockito's documentation: https://javadoc.io/doc/org.mockito/mockito-core/latest/org.mockito/org/mockito/Mockito.html#0.3
OpenJDK 64-Bit Server VM warning: Sharing is only supported for boot loader classes because bootstrap classpath has been appended
WARNING: A Java agent has been loaded dynamically (/home/denis/.m2/repository/net/bytebuddy/byte-buddy-agent/1.17.8/byte-buddy-agent-1.17.8.jar)
WARNING: If a serviceability tool is in use, please run with -XX:+EnableDynamicAgentLoading to hide this warning
WARNING: If a serviceability tool is not in use, please run with -Djdk.instrument.traceUsage for more information
WARNING: Dynamic loading of agents will be disallowed by default in a future release
Jul 31, 2026 7:23:31 PM ec.edu.espe.agrosmart.domain.ProductoFilters lambda$static$2
INFO: Producto procesado: 1 - QUINUA ORGANICA DE ALTURA
Jul 31, 2026 7:23:31 PM ec.edu.espe.agrosmart.domain.ProductoFilters lambda$static$2
INFO: Producto procesado: 2 - QUINUA ROJA ANDINA
Jul 31, 2026 7:23:31 PM ec.edu.espe.agrosmart.domain.ProductoFilters lambda$static$2
INFO: Producto procesado: 3 - HARINA DE QUINUA PREMIUM
[INFO] Tests run: 3, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 1.064 s -- in ec.edu.espe.agrosmart.service.ProductoServiceTest
[INFO] Running ec.edu.espe.agrosmart.service.PublicidadServiceTest
[INFO] Tests run: 2, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 0.056 s -- in ec.edu.espe.agrosmart.service.PublicidadServiceTest
[INFO]
[INFO] Results:
[INFO]
[INFO] Tests run: 11, Failures: 0, Errors: 0, Skipped: 0
[INFO]
[INFO] ------------------------------------------------------------------------
[INFO] BUILD SUCCESS
[INFO] ------------------------------------------------------------------------
[INFO] Total time:  2.842 s
[INFO] Finished at: 2026-07-31T19:23:31-05:00
[INFO] ------------------------------------------------------------------------


**7.2** ¿Cuántos productos espera tu `expectNextCount(...)` y por qué ese número
concreto? Relaciónalo con tu semilla.

>Para mi prueba son 3 productos ya que esta definido en el archivo ProductoServiceTest.java en la linea 29 sale .expectNextCount(3) y por que 3 pues es el que sale de mi semilla logica y el Mock del repositorio me devolvera los 5 productos en el archivo ProductoServiceTest.java en la linea 56 con el return List.of( dandome asi los 3 valores que tienen el precio mayor o igual a cero y sus correos y dos errores uno de invalido por el BigDecimal.ZERO y el otro porque no tiene un correo valido segun el mismo archivo pero en las lineas 91 que da el BigDecimal.ZERO, y 102 que da el "".

**7.3** ¿Por qué mockeaste `ProductoRepository` en lugar de dejar que la prueba consulte
PostgreSQL?

> Por que la prueba apunta a validar la logica de ProductoService y no como tal la infraestructura que tiene postgresql, asi como lo dice el propio archivo ProductoServiceTest.java en la linea 22 que da ProductoRepository repository = mock(ProductoRepository.class); que controla que datos devuelve en el mismo archivo solo que en la linea 23  when(repository.findAll()).thenReturn(productosConTresValidosYDosInvalidos());

**7.4** ¿Qué demuestra `assertNotSame` que `assertEquals` **no** demuestra en tu prueba
de copia defensiva?

>Como tal asserNotSame mediante el archivo ProductoTest.java en la linea 36 con assertNotSame(correos, producto.getCorreosNotificacion()); demuestra que por getCorreosNotificacion() nos da la lista no es la misma que tiene en memoria que la original. a diferencia de assertEquals que solo demuestra que ambas tienen el mismo contenido nada mas como se muestra en el archivo ProductoTest.java linea 27 List<String> correos = new ArrayList<>(); que nomas pasa al constructor en el archivo ProductoTest.java en la linea 29  Producto producto = new Producto(1L, "Quinua organica", "Quinua", new BigDecimal("4.80"), correos); para terminar de modificarla en la lista externa del ProductoTest.java en su linea 32 correos.add("externo@agrosmart.ec");
>Demostrando que assertEquals no basta para nada que se rompio la referencia y que assertNotSame si logra verificar que no se esta pasando el mismo objeto de la lista.

**7.5** ¿Por qué una prueba de un `Flux` que no llama a `verifyComplete()` (o a
`verify()`) no está probando nada?

>Por que el Flux de mi proyecto es lazy por ello esta el archivo ProductoServiceTest.java que en la linea 30 .verifyComplete(); fuerza la suscripcion porque sin eso el Flux solo esta construido en  ProductoServiceTest.java en la linea 26 Flux<Producto> flujo = service.obtenerProductosComercializables(); que como tal no ejecuta nada de nada si no que todo empieza gracias al StepVerifier que se suscribe y comienza todo el proceso de verificacion.

---

## Fase 8 — Integración y cierre

**8.1** Pega tu `git log --oneline --graph --all`.

denis@Dennis:~/pa/Agrosmart-final-Andrade$ git log --oneline --graph --all

* f6bd4ec (HEAD -> main, origin/main, origin/feature/documentacion, origin/HEAD, feature/documentacion) docs: documenta evidencias e instrucciones de ejecucion
* 5884c58 (origin/feature/pruebas, feature/pruebas) test: agrega pruebas del modelo, logica funcional, flujo reactivo e ia
* 3c93292 (origin/feature/api-reactiva, feature/api-reactiva) feat: expone endpoints reactivos y de publicidad
* 7b86089 (origin/feature/ia-langchain4j, feature/ia-langchain4j) feat: integra langchain4j para publicidad de productos
* f6ac5f2 (origin/feature/servicio-reactivo, feature/servicio-reactivo) feat: implementa servicio reactivo con boundedElastic y operadores
* 57062e1 (origin/feature/modelo-inmutable, feature/modelo-inmutable) feat: agrega modelo inmutable de producto y logica funcional
* 1c3d3e2 (origin/feature/persistencia-jpa, feature/persistencia-jpa) feat: agrega entidad jpa de productos y siembra de datos
* 8b40a09 (origin/feature/config-perfiles, feature/config-perfiles) chore: configura perfil prod con postgresql y puerto propio
* 172eeca Update IDENTIDAD.md with parameter values
* 68d8917 Update personal information in IDENTIDAD.md
* 4e16969 Initial commit

**8.2** ¿Qué fase te tomó más tiempo del previsto y por qué?

> La fase de agrega modelo inmutable de producto y logica funcional simplemente fue una locura ver que es inmutable y que no por ejemplo en el producto supuestamente era inmutable por que era la clase final y los atributos tambien eran private final pero ahi me tienes colocando en correosNotificacion guardando la referencia directa del constructor y diras que encontre el error rapido no me tomo sus buenos minutos encontrarlo y no fue para nada divertido ver el error.

**8.3** Si tuvieras 30 minutos más, ¿qué mejorarías **primero** de tu entrega y por qué
esa y no otra?

>En si como se hicieron las pruebas de las APIs por que quiero automatizar el proceso con WebTestClient por que actualmente mis APIs estan en AgroSmartController.java en la linea 34 @GetMapping("/productos/{id}") las pruebas cubren dominio y servicios ya ni se diga sobre el manejo del 404 que se hace en el archivo ApiExceptionHandler.java en la linea 12 @ResponseStatus(HttpStatus.NOT_FOUND) por que todo eso se hace de forma manual y muy poco practico. Con esa automatizacion lograria no solo ahorrar tiempo mas bien seria sacar constantemente informacion valiosa en segundos para saber si alguna mejora del proyecto perjudico o daño alguna parte importante y no solo limitarme a las 3 tipicas consultas que actualmente estan.

**8.4** Declara honestamente qué herramientas consultaste durante el examen
(documentación, apuntes, asistentes de IA) y para qué. **Esta declaración no descuenta
puntaje**; su omisión o falsedad sí constituye falta de honestidad académica.

>Como dije arriba sobre en "que fase te tomo mas tiempo?" para encontrar el error literalmente tuve que pasar archivo por archivo a la ia ya que me estaba desesperando y chatgpt nunca lo encontro pero claude si y en pequeñas cosas que me trababa por ejemplo en el uso de la IA una conexion se me cayo y la IA lo arreglo muy facilmente y asi entre otras cosas a lo largo del desarrollo. De ahi también use un repo que tenia una guia de como usar correctamente las APIs:https://github.com/spring-guides/gs-reactive-rest-service?utm_source=chatgpt.com también use un tutorial de youtube para ver como usar la IA LangChain4j ya que era bastante completo la verdad tenia todos los pasos claro enfocado a otro tema pero igual sirvió bastante.
